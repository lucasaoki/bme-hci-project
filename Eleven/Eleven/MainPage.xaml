<Page
    x:Name="pageRoot"
    x:Class="Eleven.MainPage"
    DataContext="{Binding DefaultViewModel, RelativeSource={RelativeSource Self}}"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Eleven"
    xmlns:data="using:Eleven.Data"
    xmlns:common="using:Eleven.Common"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <!--
            Collection of grouped items displayed by this page, bound to a subset
            of the complete item list because items in groups cannot be virtualized
        -->
        <CollectionViewSource
            x:Name="groupedItemsViewSource"
            Source="{Binding Groups}"
            IsSourceGrouped="true"
            ItemsPath="Items"
            d:Source="{Binding Groups, Source={d:DesignData Source=/DataModel/Database.json, Type=data:DataSource}}"/>
    </Page.Resources>

    <Grid Background="{ThemeResource ButtonBackgroundThemeBrush}" >
        <Grid.ChildrenTransitions>
            <TransitionCollection>
                <EntranceThemeTransition/>
            </TransitionCollection>
        </Grid.ChildrenTransitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="140"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="350"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Row="2" Grid.ColumnSpan="1" 
              Background="DarkSlateGray" >

            <StackPanel VerticalAlignment="Top">

                <Grid x:Name="Home_Grid" Tapped="Home_Select_Click" PointerMoved="Home_PointerMoved" Height="150">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="100"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Grid Grid.RowSpan="1">
                        <Grid.Background>
                            <ImageBrush Stretch="Uniform" ImageSource="Assets/Logo.png"/>
                        </Grid.Background>
                    </Grid>
                    <Grid Grid.RowSpan="2">
                        <TextBlock x:Name="Home_TextBox"  Text="Home"  FontSize="30" FontFamily="Garamond" 
                            HorizontalAlignment="Left" VerticalAlignment="Bottom" Width="350" Height="40" FontWeight="Normal" Padding="50, 0 ,0 ,0"  Foreground="{ThemeResource ListViewItemOverlayForegroundThemeBrush}" 
                            Style="{StaticResource TitleTextBlockStyle}"  Margin="0,0,-10,0"/>
                    </Grid>
                </Grid>

                
                <Grid x:Name="Artwork_Grid" Tapped="ArtWork_Select_Click" PointerMoved="Artwork_PointerMoved" Height="150">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="100"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Grid Grid.RowSpan="1">
                        <Grid.Background>
                            <ImageBrush Stretch="Uniform" ImageSource="Assets/Logo.png"/>
                        </Grid.Background>
                    </Grid>
                    <Grid Grid.RowSpan="2">
                        <TextBlock x:Name="ArtWork_TextBox"  Text="Painting"  FontSize="30" FontFamily="Garamond" 
                            HorizontalAlignment="Left" VerticalAlignment="Bottom" Width="350" Height="40" FontWeight="Normal" Padding="50, 0 ,0 ,0"  Foreground="{ThemeResource ListViewItemOverlayForegroundThemeBrush}" 
                            Style="{StaticResource TitleTextBlockStyle}"  Margin="0,0,-10,0"/>
                    </Grid>
                </Grid>


                <Grid x:Name="Period_Grid" Tapped="Period_Select_Click" PointerMoved="Period_PointerMoved" Height="150">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="100"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                        <Grid Grid.RowSpan="1">
                            <Grid.Background>
                                <ImageBrush Stretch="Uniform" ImageSource="Assets/Logo.png"/>
                            </Grid.Background>
                        </Grid>
                        <Grid Grid.RowSpan="2">
                        <TextBlock x:Name="Period_TextBox"  Text="Period"  FontSize="30" FontFamily="Garamond" 
                            HorizontalAlignment="Left" VerticalAlignment="Bottom" Width="350" Height="40" FontWeight="Normal" Padding="50, 0 ,0 ,0"  Foreground="{ThemeResource ListViewItemOverlayForegroundThemeBrush}" 
                            Style="{StaticResource TitleTextBlockStyle}"  Margin="0,0,-10,0"/>
                        </Grid>
                    </Grid>

                <Grid x:Name="Artist_Grid" Tapped="Artist_Select_Click" PointerMoved="Artist_PointerMoved" Height="150">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="100"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Grid Grid.RowSpan="1">
                        <Grid.Background>
                            <ImageBrush Stretch="Uniform" ImageSource="Assets/Logo.png"/>
                        </Grid.Background>
                    </Grid>
                    <Grid Grid.RowSpan="2">
                        <TextBlock x:Name="Artist_TextBox"  Text="Artist"  FontSize="30" FontFamily="Garamond" 
                            HorizontalAlignment="Left" VerticalAlignment="Bottom" Width="350" Height="40" FontWeight="Normal" Padding="50, 0 ,0 ,0"  Foreground="{ThemeResource ListViewItemOverlayForegroundThemeBrush}" 
                            Style="{StaticResource TitleTextBlockStyle}"  Margin="0,0,-10,0"/>
                    </Grid>
                </Grid>

                <Grid x:Name="Sculpture_Grid" Tapped="Sculpture_Select_Click" PointerMoved="Sculpture_PointerMoved" Height="150">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="100"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Grid Grid.RowSpan="1">
                        <Grid.Background>
                            <ImageBrush Stretch="Uniform" ImageSource="Assets/Logo.png"/>
                        </Grid.Background>
                    </Grid>
                    <Grid Grid.RowSpan="2">
                        <TextBlock x:Name="Sculpture_TextBox"  Text="Sculpture"  FontSize="30" FontFamily="Garamond" 
                            HorizontalAlignment="Left" VerticalAlignment="Bottom" Width="350" Height="40" FontWeight="Normal" Padding="50, 0 ,0 ,0"  Foreground="{ThemeResource ListViewItemOverlayForegroundThemeBrush}" 
                            Style="{StaticResource TitleTextBlockStyle}"  Margin="0,0,-10,0"/>
                    </Grid>
                </Grid>
            </StackPanel>
        </Grid>

        <Grid Grid.Column="2" 
              Grid.Row="2" 
              Background="SlateGray">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="820"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <GridView
                x:Name="itemGridView2"
                Padding="50,100,50,50"
                SelectionMode="None"
                IsSwipeEnabled="false"
                IsItemClickEnabled="True"
                ItemClick="ItemView_ItemClick" 
                Margin="0,0,5,0" 
                ScrollViewer.HorizontalScrollBarVisibility="Disabled">

                <Grid HorizontalAlignment="Center" Width="700" Height="560">
                    <Border Background="{ThemeResource ListViewItemPlaceholderBackgroundThemeBrush}">
                        <Image Source="Assets/GoBack.jpg" Stretch="Uniform" VerticalAlignment="Top" Margin="0,0,0,0"/>
                    </Border>
                    <StackPanel VerticalAlignment="Bottom" Background="{ThemeResource ListViewItemOverlayBackgroundThemeBrush}">
                        <TextBlock Text="Hello" Foreground="{ThemeResource ListViewItemOverlayForegroundThemeBrush}" Style="{StaticResource TitleTextBlockStyle}" Height="100" Margin="15,0,15,0"/>
                    </StackPanel>
                </Grid>
                <GridView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <ItemsWrapGrid GroupPadding="0,0,0,0"/>
                    </ItemsPanelTemplate>
                </GridView.ItemsPanel>
            </GridView>

            <GridView
                x:Name="itemGridView"
                Padding="0,100,0,0"
                ItemsSource="{Binding Source={StaticResource groupedItemsViewSource}}"
                SelectionMode="None"
                IsSwipeEnabled="false"
                IsItemClickEnabled="True"
                ItemClick="ItemView_ItemClick"
                SelectionChanged="itemGridView_SelectionChanged"
                Grid.Column="1" Margin="0,0,0.429,0" >

                <GridView.ItemTemplate>
                    <DataTemplate>
                        <Grid HorizontalAlignment="Left" Width="300" Height="275">
                            <Border Background="{ThemeResource ListViewItemPlaceholderBackgroundThemeBrush}">
                                <Image Source="{Binding ImagePath}" Stretch="Uniform" VerticalAlignment="Top"/>
                            </Border>
                            <StackPanel VerticalAlignment="Bottom" Background="{ThemeResource ListViewItemOverlayBackgroundThemeBrush}">
                                <TextBlock Text="{Binding Artist}" Foreground="{ThemeResource ListViewItemOverlayForegroundThemeBrush}" Style="{StaticResource TitleTextBlockStyle}" Height="60" Margin="15,0,15,0"/>
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </GridView.ItemTemplate>

                <GridView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <ItemsWrapGrid GroupPadding="0,0,0,0"/>
                    </ItemsPanelTemplate>
                </GridView.ItemsPanel>
            </GridView>

        </Grid>

        <Grid Grid.Column="2" Grid.RowSpan="1" HorizontalAlignment="Right" Width="296">
            <!--Search Block-->
            <Grid Height="40"  VerticalAlignment="Center" Margin="0,60,0,40" >
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="50"/>
                </Grid.ColumnDefinitions>
                <Grid Grid.Column="0" Grid.ColumnSpan="2" Height="40" VerticalAlignment="Top"  >
                    <StackPanel  Orientation="Horizontal" Margin="0,20,0,20">
                        <TextBox x:Name="SearchInput" Background="Transparent" TextAlignment="Right" SelectionHighlightColor="AntiqueWhite" Foreground="White"  Margin="0,-18" Height="36" FontSize="20" Width="250" BorderThickness="0,0,0,0"/>
                    </StackPanel>
                </Grid>
                <Grid Grid.Column="2">
                    <Button>
                        <Image Source="Assets/Search.jpg" Stretch="Uniform"/>
                    </Button>
                </Grid>

            </Grid>
        </Grid>

        <Grid Grid.RowSpan="1" Grid.ColumnSpan="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="600"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!--GoBack Buttonm-->
            <Button x:Name="backButton" Margin="39,59,39,0" Command="{Binding NavigationHelper.GoBackCommand, ElementName=pageRoot}"
                        Style="{StaticResource NavigationBackButtonNormalStyle}"
                        VerticalAlignment="Top"
                        AutomationProperties.Name="Back"
                        AutomationProperties.AutomationId="BackButton"
                        AutomationProperties.ItemType="Navigation Button"/>

            <!--Title-->
            <TextBlock x:Name="pageTitle" Text="Eleven" Style="{StaticResource HeaderTextBlockStyle}"  Grid.Column="1" 
                        IsHitTestVisible="false" TextWrapping="NoWrap" VerticalAlignment="Bottom" Margin="0,0,30,40"/>

            <!--Grid top right-->

        </Grid>


    </Grid>
</Page>
