<Page
    x:Name="pageRoot"
    x:Class="Eleven.MainPage"
    DataContext="{Binding DefaultViewModel, RelativeSource={RelativeSource Self}}"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Eleven"
    xmlns:data="using:Eleven.Data"
    xmlns:common="using:Eleven.Common"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <!--
            Collection of grouped items displayed by this page, bound to a subset
            of the complete item list because items in groups cannot be virtualized
        -->
        <CollectionViewSource
            x:Name="groupedItemsViewSource"
            Source="{Binding Groups}"
            IsSourceGrouped="true"
            ItemsPath="Items"
            d:Source="{Binding Groups, Source={d:DesignData Source=/DataModel/Database.json, Type=data:DataSource}}"/>
    </Page.Resources>

    <Grid Background="{ThemeResource ButtonBackgroundThemeBrush}" >
        <Grid.ChildrenTransitions>
            <TransitionCollection>
                <EntranceThemeTransition/>
            </TransitionCollection>
        </Grid.ChildrenTransitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="140"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="350"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Row="2" Grid.ColumnSpan="1" 
              Background="DarkSlateGray">

            <StackPanel  VerticalAlignment="Center"  Margin="0,60,0,154" MinWidth="1" Height="414">

                <TextBlock x:Name="ArtWork_TextBox" Text="Painting" Tapped="ArtWork_Select_Click" Foreground="Beige" FontSize="32" FontFamily="Garamond" 
        			HorizontalAlignment="Center" VerticalAlignment="Center" Width="300" Height="80" FontWeight="Normal" LineHeight="6" Padding="50,20,0,0">
                </TextBlock>

                <TextBlock x:Name="Artist_TextBox" Text="Artist" Tapped="Artist_Select_Click" Foreground="Beige" FontSize="32" FontFamily="Garamond" 
        			HorizontalAlignment="Center" VerticalAlignment="Center" Width="300" Height="80" FontWeight="Normal" LineHeight="6"  Padding="50,20,0,0">
                </TextBlock>
                <TextBlock x:Name="Period_TextBox" Text="Period" Tapped="Period_Select_Click" Foreground="Beige" FontSize="32" FontFamily="Garamond" 
        			HorizontalAlignment="Center" VerticalAlignment="Center" Width="300" Height="80" FontWeight="Normal" LineHeight="6"  Padding="50,20,0,0">
                </TextBlock>
                
                <TextBlock x:Name="Sculpture_TextBox" Text="Sculpture" Tapped="Sculpture_Select_Click" Foreground="Beige" FontSize="32" FontFamily="Garamond" 
        			HorizontalAlignment="Center" VerticalAlignment="Center" Width="300" Height="80" FontWeight="Normal" LineHeight="6" Padding="50,20,0,0">
                </TextBlock>
            </StackPanel>
        </Grid>

        <Grid Grid.Column="2" 
              Grid.Row="2" 
              Background="SlateGray">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="600"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <GridView
                x:Name="itemGridView2"
                Padding="50,100,50,50"
                SelectionMode="None"
                IsSwipeEnabled="false"
                IsItemClickEnabled="True"
                ItemClick="ItemView_ItemClick" 
                Margin="0,0,5,0" 
                ScrollViewer.HorizontalScrollBarVisibility="Disabled">

                <Grid HorizontalAlignment="Center" Width="490" Height="560">
                    <Border Background="{ThemeResource ListViewItemPlaceholderBackgroundThemeBrush}">
                        <Image Source="Assets/GoBack.jpg" Stretch="UniformToFill" VerticalAlignment="Top"/>
                    </Border>
                    <StackPanel VerticalAlignment="Bottom" Background="{ThemeResource ListViewItemOverlayBackgroundThemeBrush}">
                        <TextBlock Text="Hello" Foreground="{ThemeResource ListViewItemOverlayForegroundThemeBrush}" Style="{StaticResource TitleTextBlockStyle}" Height="100" Margin="15,0,15,0"/>
                    </StackPanel>
                </Grid>
                <GridView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <ItemsWrapGrid GroupPadding="0,0,0,0"/>
                    </ItemsPanelTemplate>
                </GridView.ItemsPanel>
            </GridView>

            <GridView
                x:Name="itemGridView"
                Padding="50,100,0,0"
                ItemsSource="{Binding Source={StaticResource groupedItemsViewSource}}"
                SelectionMode="None"
                IsSwipeEnabled="false"
                IsItemClickEnabled="True"
                ItemClick="ItemView_ItemClick"
                SelectionChanged="itemGridView_SelectionChanged"
                Grid.Column="1" >



                <GridView.ItemTemplate>
                    <DataTemplate>
                        <Grid HorizontalAlignment="Left" Width="300" Height="275">
                            <Border Background="{ThemeResource ListViewItemPlaceholderBackgroundThemeBrush}">
                                <Image Source="{Binding ImagePath}" Stretch="Uniform" VerticalAlignment="Top"/>
                            </Border>
                            <StackPanel VerticalAlignment="Bottom" Background="{ThemeResource ListViewItemOverlayBackgroundThemeBrush}">
                                <TextBlock Text="{Binding Artist}" Foreground="{ThemeResource ListViewItemOverlayForegroundThemeBrush}" Style="{StaticResource TitleTextBlockStyle}" Height="60" Margin="15,0,15,0"/>
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </GridView.ItemTemplate>

                <GridView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <ItemsWrapGrid GroupPadding="0,0,0,0"/>
                    </ItemsPanelTemplate>
                </GridView.ItemsPanel>
            </GridView>

        </Grid>

        <Grid Grid.Column="2" Grid.RowSpan="1" HorizontalAlignment="Right" Width="296">
            <!--Search Block-->
            <Grid Height="40"  VerticalAlignment="Center" Margin="0,60,0,40" >
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="250"/>
                </Grid.ColumnDefinitions>
                <Grid Grid.Column="0" Grid.ColumnSpan="2" Height="40" VerticalAlignment="Top"  >
                    <StackPanel  Orientation="Horizontal" Margin="0,20,0,20">
                        <TextBox x:Name="SearchInput" Background="Transparent" Foreground="White"  Margin="0,-18" Height="36" FontSize="20" Width="250" BorderThickness="0,0,0,0"/>
                    </StackPanel>
                </Grid>

                <Grid Grid.Column="1" Margin="209,0,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition/>
                    </Grid.RowDefinitions>
                    <Button Margin="0,8">
                        <Image Source="Assets/Search.jpg" Stretch="Fill"/>
                    </Button>
                </Grid>
            </Grid>
        </Grid>

        <Grid Grid.RowSpan="1" Grid.ColumnSpan="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="600"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!--GoBack Buttonm-->
            <Button x:Name="backButton" Margin="39,59,39,0" Command="{Binding NavigationHelper.GoBackCommand, ElementName=pageRoot}"
                        Style="{StaticResource NavigationBackButtonNormalStyle}"
                        VerticalAlignment="Top"
                        AutomationProperties.Name="Back"
                        AutomationProperties.AutomationId="BackButton"
                        AutomationProperties.ItemType="Navigation Button"/>

            <!--Title-->
            <TextBlock x:Name="pageTitle" Text="Eleven" Style="{StaticResource HeaderTextBlockStyle}"  Grid.Column="1" 
                        IsHitTestVisible="false" TextWrapping="NoWrap" VerticalAlignment="Bottom" Margin="0,0,30,40"/>

            <!--Grid top right-->

        </Grid>


    </Grid>
</Page>
