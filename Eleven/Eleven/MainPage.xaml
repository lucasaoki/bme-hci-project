<Page
    x:Name="pageRoot"
    x:Class="Eleven.MainPage"
    DataContext="{Binding DefaultViewModel, RelativeSource={RelativeSource Self}}"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Eleven"
    xmlns:data="using:Eleven.Data"
    xmlns:common="using:Eleven.Common"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <!--
            Collection of grouped items displayed by this page, bound to a subset
            of the complete item list because items in groups cannot be virtualized
        -->
        <CollectionViewSource
            x:Name="groupedItemsViewSource"
            Source="{Binding Groups}"
            IsSourceGrouped="true"
            ItemsPath="Items"
            d:Source="{Binding Groups, Source={d:DesignData Source=/DataModel/Database.json, Type=data:DataSource}}"/>
    </Page.Resources>

    <Grid Background="{ThemeResource ButtonBackgroundThemeBrush}" >
        <Grid.ChildrenTransitions>
            <TransitionCollection>
                <EntranceThemeTransition/>
            </TransitionCollection>
        </Grid.ChildrenTransitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="140"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.RowSpan="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="400"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <GridView
            x:Name="itemListView" Padding="0,10,0,20"  HorizontalAlignment="Center"  RequestedTheme="Dark" Margin="45,185,45,63" SelectionMode="None">

                <Button x:Name="ArtWork_TextBox" Content="ArtWork"  Click="ArtWork_Select_Click" Foreground="Beige" FontSize="30" FontFamily="Arial" 
                            HorizontalAlignment="Center" VerticalAlignment="Stretch" Width="300" Height="80" BorderThickness="0,0,0,0" Background="{ThemeResource AppBarItemDisabledForegroundThemeBrush}"/>

                <Button x:Name="Artist_TextBox"  Content="Artist" Click="Artist_Select_Click" Foreground="Beige" FontSize="30" FontFamily="Arial" 
                            HorizontalAlignment="Center" VerticalAlignment="Stretch"  Width="300" Height="80" BorderThickness="0,0,0,0" />


                <Button x:Name="Period_TextBox" Content="Period" Click="Period_Select_Click" Foreground="Beige" FontSize="30" FontFamily="Arial" 
                            HorizontalAlignment="Center" VerticalAlignment="Stretch"  Width="300" Height="80" BorderThickness="0,0,0,0"/>

                <Button x:Name="Sculpture_TextBox" Content="Sculpture" Click="Sculpture_Select_Click" Foreground="Beige" FontSize="30" FontFamily="Arial" 
                            HorizontalAlignment="Center" VerticalAlignment="Stretch" Width="300" Height="80" BorderThickness="0,0,0,0"/>


            </GridView>

            <GridView
        x:Name="itemGridView"
            Padding="116,137,40,46"
            ItemsSource="{Binding Source={StaticResource groupedItemsViewSource}}"
            SelectionMode="None"
            IsSwipeEnabled="false"
            IsItemClickEnabled="True"
            ItemClick="ItemView_ItemClick" 
            Grid.Column="1" 
            SelectionChanged="itemGridView_SelectionChanged">
                <GridView.ItemTemplate>
                    <DataTemplate>
                        <Grid HorizontalAlignment="Left" Width="300" Height="275">
                            <Border Background="{ThemeResource ListViewItemPlaceholderBackgroundThemeBrush}">
                                <Image Source="{Binding ImagePath}" Stretch="Uniform"/>
                            </Border>
                            <StackPanel VerticalAlignment="Bottom" Background="{ThemeResource ListViewItemOverlayBackgroundThemeBrush}">
                                <TextBlock Text="{Binding Artist}" Foreground="{ThemeResource ListViewItemOverlayForegroundThemeBrush}" Style="{StaticResource TitleTextBlockStyle}" Height="60" Margin="15,0,15,0"/>
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </GridView.ItemTemplate>
                <GridView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <ItemsWrapGrid GroupPadding="0,0,0,0"/>
                    </ItemsPanelTemplate>
                </GridView.ItemsPanel>
            </GridView>
        </Grid>


        <Grid Grid.RowSpan="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="600"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!--GoBack Buttonm-->
            <Button x:Name="backButton" Margin="39,59,39,0" Command="{Binding NavigationHelper.GoBackCommand, ElementName=pageRoot}"
                        Style="{StaticResource NavigationBackButtonNormalStyle}"
                        VerticalAlignment="Top"
                        AutomationProperties.Name="Back"
                        AutomationProperties.AutomationId="BackButton"
                        AutomationProperties.ItemType="Navigation Button"/>

            <!--Title-->
            <TextBlock x:Name="pageTitle" Text="Eleven" Style="{StaticResource HeaderTextBlockStyle}"  Grid.Column="1" 
                        IsHitTestVisible="false" TextWrapping="NoWrap" VerticalAlignment="Bottom" Margin="0,0,30,40"/>

            <!--Grid top right-->
            <Grid Grid.Column="2" HorizontalAlignment="Right" Width="296">
                <!--Search Block-->
                <Grid Height="40"  VerticalAlignment="Center" Margin="0,34,0,66" >
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="250"/>
                    </Grid.ColumnDefinitions>
                    <Grid Grid.Column="0" Grid.ColumnSpan="2" Height="40" VerticalAlignment="Top"  >
                        <StackPanel  Orientation="Horizontal" Margin="0,20,0,20">
                            <TextBox x:Name="SearchInput" Background="Transparent" Foreground="White"  Margin="0,-18" Height="36" FontSize="20" Width="250" BorderThickness="0,0,0,0"/>
                        </StackPanel>
                    </Grid>

                    <Grid Grid.Column="1" Margin="209,0,0,0">
                        <Grid.RowDefinitions>
                            <RowDefinition/>
                        </Grid.RowDefinitions>
                        <Button Margin="0,8">

                            <Image Source="Assets/Search.jpg" Stretch="Fill"/>
                        </Button>
                    </Grid>
                </Grid>
            </Grid>
        </Grid>


    </Grid>
</Page>
